# Usefull-scripts
psFileMover.ps1 - moves files from subfolders to main directory and deletes empty directories

removeDuplicates.ps1 - removes duplicate files, based on same hashes, leaves files with longes file name
